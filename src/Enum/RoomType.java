//An enum to determine a room's type.
//This would likely be implemented like item type with a switch case in order to determine the room, and thus the options presented to the character.
package Enum;

public enum RoomType {
	HALLWAY,
	BASEMENT,
	BEDROOM,
	KITCHEN,
	STAIRS
}
