//An enum to determine the item's type.
//I would imagine this would be used in a switch statement to then take the appropriate actions with the given item.
package Enum;

public enum ItemType {
	GOLDKEY,
	RUSTYKEY,
	BOX,
	FLASHLIGHT	
}
